import { describe, expect, it } from "vitest";
import { providerConfigSchema, providerSecretSchema } from "@/modules/providers/contracts";
import { openSecret, sealSecret } from "@/modules/config/envelope";
describe("provider configuration",()=>{it("fails closed on an unknown schema version",()=>{expect(providerConfigSchema.safeParse({type:"resend",schemaVersion:2,apiBase:"https://api.resend.com"}).success).toBe(false)});it("fails closed when credential type does not match a supported shape",()=>{expect(providerSecretSchema.safeParse({type:"sendgrid",apiKey:""}).success).toBe(false)});it("uses resource and version as envelope AAD",()=>{const envelope=sealSecret({type:"mock",token:"secret"},"account-a",1);expect(openSecret(envelope,"account-a",1)).toEqual({type:"mock",token:"secret"});expect(()=>openSecret(envelope,"account-b",1)).toThrow()});it("fails closed when an envelope references an unavailable KEK version",()=>{const envelope=sealSecret({token:"secret"},"account-a",1);expect(()=>openSecret({...envelope,keyVersion:"missing-version"},"account-a",1)).toThrow(/No KEK is configured/)})});
